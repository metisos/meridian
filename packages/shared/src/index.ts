/**
 * @meridian/shared — cross-package types
 *
 * Real types land here as packages/contextsync and packages/usc start defining them.
 * For now this is a placeholder that exports a single name so the package resolves.
 */

export const SHARED_PACKAGE = "@meridian/shared" as const;
